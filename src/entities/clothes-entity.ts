export class Clothes {
    shirt: string;
    pant: string;
    sock: string;

    constructor(shirt: string, pant: string, sock: string) {
        this.shirt = shirt;
        this.pant = pant;
        this.sock = sock;
    }
}